---
title: "PHP"
date: 2015-08-05
lastmod: 2018-04-19
draft: false
gist_id: "3c6dbfd9e696af0f6f60"
gist_url: "https://gist.github.com/hdknr/3c6dbfd9e696af0f6f60"
categories: ["Web開発"]
tags: []
---

## もろもろ

### 文字数とか

- [PHPでの文字列の文字数・バイト数・文字幅（見た目の長さ）の数え方](http://blog.ishitoya.info/entry/20090707/1246970868)

### 全角チェック

~~~php
<?php
    /**
     * @assert ('文字列です。') === true
     * @assert ('abcd1234+:;!') === false
     * @assert ('全角abcd') === false
     * @assert ('半角ｶﾅ') === false
     */
    function is_zen_preg_match ($str) {
        if (!preg_match("/(?:\xEF\xBD[\xA1-\xBF]|\xEF\xBE[\x80-\x9F])|[\x20-\x7E]/", $str)) {
            return true;
        } else {
            return false;
        }
    }
~~~



### 名前でメソッドを呼ぶ

~~~php
<?php

class Checker {
    function check_name($value) {
        return false;
    }
}

$profile = array(
    'name' => 'my name',
    'phone' => '333-3333-3333'
);

$checker = new Checker();

function check($checker, $key, $value ){
    $method = "check_$key";
    if(method_exists($checker, $method)){
        return $checker->$method($value);
    }
    return true;
}

foreach($profile as $key => $value ){
    echo $key, ":", check($checker, $key, $value ), "\n";
}
~~~


### map関数

~~~php
<?php

$array = array('a' => 1, 'b' => 2, 'c' => 3);

$array2 = array_map(function($val){ return $val * 2 ; }, $array);

foreach($array as  $k => $v ){
    echo "$k .... $v\n";
}

foreach($array2 as  $k => $v ){
    echo "$k .... $v\n";
}
~~~

~~~bash
$ php tests_map.php
a .... 1
b .... 2
c .... 3
a .... 2
b .... 4
c .... 6
~~~


### インスタンスクラスのstatic メンバーへのアクセス

~~~php

$model_class = get_class($instance);
$value = $model_class::$static_value;
~~~

### 配列要素の削除

~~~php
unset($array[$i]);
~~~



- [PHP docs](https://github.com/hdknr/scriptogr.am/tree/master/php)
- [PHP: Noticeエラーを回避してarrayから値を取得するいくつかの例 ](https://gist.github.com/nissuk/954926)

## mysql

-接続確認

~~~
# /vagrant/php/vendor/bin/psysh
Psy Shell v0.5.2 (PHP 5.5.27 ― cli) by Justin Hileman
>>> (new mysqli('localhost', 'root', 'password'))->query('show databases')->fetch_all();
~~~

## error 

- php.ini

~~~
display_errors = on
error_reporting = E_ALL
~~~

## templates

[TOP 5 PHP TEMPLATE ENGINES](http://www.sitecrafting.com/blog/top-5-php-template-engines/)


- [Mustache](https://mustache.github.io/)
- [Blade](http://laravel.com/docs/5.0/templates) Haml / Jadeインスパイア (Laravel)
- [Smarty](http://www.smarty.net/)
- [Twig](http://twig.sensiolabs.org/) Jinjaインスパイア (Symfony)
- [Volt](https://docs.phalconphp.com/ja/latest/reference/volt.html) Jinjaインスパイア (Phalcon)
