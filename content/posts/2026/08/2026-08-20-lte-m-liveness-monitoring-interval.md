---
title: "「1 分間通信がなければアラート」は作れるか — LTE-M 機の死活監視を電池から逆算する"
date: 2026-08-20
lastmod: 2026-08-20
slug: "lte-m-liveness-monitoring-interval"
description: "SORACOM Lagoon 3 の no data アラートで「一定時間データが来なければ通知」は作れる。だが LTE-M 電池駆動機では 1 分周期が電池 3.3 時間で尽きる。送信間隔・電池寿命・検知遅延・無通信タイマーの 4 つを突き合わせて、死活監視の間隔を決める手順をまとめる。"
draft: false
categories: ["クラウド/インフラ"]
tags: ["IoT", "SORACOM", "LTE-M", "監視", "遠隔監視"]
---

[前回](/blogs/posts/2026/08/lte-session-reconnect-iot-gateway/)、IoT ゲートウェイのセッションが `Deleted` → `Created` を繰り返す現象を 4 層で切り分ける話を書いた。その続きで、こういう質問をもらった。

> SORACOM 側で、1 分間 SmartFitPRO から通信がなかったらアラートを出す、ということはできますか？

答えは「**SORACOM 側は作れる。デバイス側が持たない**」だ。そして持たない理由を数字で追うと、死活監視の間隔をどう決めるべきかという一般的な話に行き着く。

結論だけ先に置くと、電池駆動の LTE-M 機で 1 分周期の死活監視をやると、**電池は 3.3 時間で切れる**。監視のために監視対象を殺す設定になる。

## SORACOM 側は「データが来ない」をアラートにできる

まず、できる方の話から。SORACOM Lagoon 3 の Alert rule には、データが存在しない場合の扱いを決める設定項目がある。名前は **`Alert state if no data or all values are null`** で、ここを Alerting にすれば「データが来ていない」がそのまま発火条件になる。

Alert rule の主な設定項目はこうなっている。

| 項目 | 内容 |
| --- | --- |
| `Evaluate every` | 評価間隔。「10 秒の倍数を入力してください (例: `1m`)」 |
| `for` | 継続時間。条件成立が継続した時間で Firing に変わる。`0` なら 1 回で発火 |
| `From` / `To` | 評価範囲。どちらも `now` を含む相対時間で指定（`now-5m` など） |
| `Alert state if no data or all values are null` | 無データ時の状態。ここが死活監視の本体 |

`Evaluate every` は 10 秒の倍数なので、`1m` は指定できる。ここだけ見れば「1 分間データが来なければアラート」は作れる。

ただし落とし穴が 1 つある。Lagoon は指定間隔で Harvest Data からデータを取得しており、**その取得間隔がプランごとに違う**。

| プラン | データ取得間隔 | Alert rule 数 |
| --- | --- | --- |
| Free | 60 秒 | 1 |
| Maker | 30 秒 | 10 |
| Pro | 5 秒 | 20 |

評価範囲を取得間隔より `now` に近く取ると、Lagoon がまだデータを取得しておらず No data と判定されてしまう。Free プラン（取得間隔 60 秒）で `now-1m` を評価範囲にするのは、ちょうどこの境界を踏む設定だ。1 分でやるなら Maker 以上にするか、評価範囲を少し広げる。

**なお、イベントハンドラーでは 1 分はできない。** ルールタイプに `SimSessionStatusRule` があり `Created` / `Deleted` を拾えるが、`Deleted` が出るのは無通信タイマーが満了した後だ。この時間は 1 分ではない。

## デバイス側の予算は「時間」ではなく「回数」で決まっている

ここからが本題だ。SmartFitPRO Basic の電気仕様を取扱説明書から引くと、こうなっている。

| 項目 | スペック |
| --- | --- |
| 電源部 | 単 4 形乾電池 直列 2 本（動作保証電圧 2.6V–3.3V）／ USB Type-C（5V） |
| 無線部 | LTE Cat.M1（Docomo / KDDI 対応） |
| SIM カード | Micro サイズ SIM（SORACOM plan-D を使用） |
| **連続稼働時間** | **単 4 形乾電池で 200 回程度 LTE 送信** |
| 消費電力 | 通信時 ≤ 300mA / 待機 ≤ 100μA |

注目すべきは、連続稼働時間が**日数ではなく送信回数で書かれている**ことだ。これは表記の都合ではなく、この機器の電池寿命が経過時間ではなく送信回数で決まることを意味している。待機時 100μA というのは、置いておくだけならほとんど減らない水準だ。電池を食うのは、LTE の起動・送信・切断を 1 回やること自体である。

つまり **200 回という送信予算があり、送信間隔を決めた瞬間に稼働日数が決まる**。逆算はただの割り算になる。

![SmartFitPRO Basic の定期送信間隔ごとに、電池寿命の目安・死活監視の検知遅延・無通信タイマー 1 時間との関係を並べた表。1 分間隔では電池が約 3.3 時間しかもたず、1 時間を超えると毎周期 Deleted と Created が記録されることを示している](/blogs/images/lte-m-liveness-interval-battery-budget.png)

この表から 3 つのことが読み取れる。

### 1 分の死活監視は成立しない

1 分間隔なら 200 分、**約 3.3 時間**で電池が尽きる。5 分でも 17 時間で、1 日もたない。死活監視のために監視対象を殺しているので、これは設定ミスというより設計の不整合だ。

そもそも定期送信の間隔は自由に変えられない。取扱説明書には「※定期送信時間を変更されたい場合はお問い合わせ願います」とあり、付属の設定ツールで詰める項目ではなくメーカー相談事項になっている。

1 分粒度が本当に必要なら、電池モデルではなく給電モデルを選ぶ。SmartFitPRO は型番で電源方式が分かれていて、待機電流が桁で違う。

| モデル | 動作電源 | 通信時 | 待機 |
| --- | --- | --- | --- |
| Basic | 単 4 形乾電池 2 本 / USB Type-C | ≤ 300mA | **≤ 100μA** |
| Standard | USB Type-C | ≤ 150mA | ≤ 35mA |
| Advance | DC 12–24V / USB Type-C | ≤ 100mA | ≤ 20mA |

Basic の待機 100μA に対し、Standard は 35mA。**350 倍**である。この差が「電池で動くかどうか」を決めていて、逆に言えば Standard / Advance は常時給電が前提なので、送信間隔を詰めても電池の心配はない。無線モジュールも Basic / Standard / Advance すべて LTE-M（Cat.M1）が選べ、Advance だけ LTE（Cat.1）も選べる。

### 電池運用なら `Deleted` → `Created` は避けられない

ここで前回の記事と話がつながる。

**plan-D の無通信タイマーは 1 時間**だ（SORACOM のサブスクリプション一覧に明記されている。plan01s や planX3 は「1 時間または 4 時間」で、ランデブーポイントがフランクフルトかシドニーなら 1 時間、それ以外は 4 時間という条件付き。なお「セッションの無通信タイマーは予告なく変更される可能性があります」という注記も付いている）。

電池で実用になる送信間隔は、上の表のとおり数時間から 1 日だ。そしてそれは**必ず無通信タイマーの 1 時間より長い**。つまり、

- 送信 → セッション確立
- 1 時間の無通信で `Deleted`
- 次の送信タイミングで `Created`

が毎周期繰り返される。**電池運用を選んだ時点で、ログに `Deleted` → `Created` が並ぶことは確定している。** これは異常ではなく、前回書いた「送信周期と一致する `Deleted`」の教科書的なパターンそのものだ。

逆に言えば、送信間隔を 1 時間より短くすればセッションは維持されるが、そのとき電池は 8 日もたない。**セッションを維持するか、電池を延ばすか**という二択になっていて、電池駆動機では後者を選ぶしかない。

### 検知遅延は仕様として引き受ける

死活監視のアラート閾値は、定期送信間隔の 2〜3 倍に置くのがセオリーだ。1 回の取りこぼしで鳴らすと、電波の揺らぎで誤報を量産する。

すると 24 時間間隔なら、機器が死んだことを知るのは**最短でも 48 時間後**になる。これは受け入れるしかない。死活監視の検知遅延が送信間隔より短くなることは、原理的にありえない。

## 異常検知と死活監視は別のアラートにする

「48 時間後では困る」という反応が当然出る。ここで効くのが、送信契機の区別だ。

SmartFitPRO が Harvest Data に送る JSON には `message_type` が入っていて、3 つの契機を区別できる。

| `message_type` | 意味 |
| --- | --- |
| `0001` | 起動送信 |
| `0002` | 周期送信 |
| `0003` | 接点変化送信 |

送信データのフォーマットは JSON で、ほかに `Devicetype`、`fw_ver`、`Timestamp`（UNIX エポック秒）、`device_state`（正常 0 / エラーあり 1）、`di0`（0: 開 / 1: 閉）が入る。

重要なのは **`0003` の接点変化送信が、変化した時点で飛ぶ**ことだ。定期送信を待たない。つまり「異常を早く知りたい」という要求は、死活監視の間隔とは無関係に満たされている。

![SmartFitPRO の 3 種類の送信契機を Harvest Data 経由で Lagoon の 2 本の Alert rule に振り分ける構成図。接点変化送信は異常検知として即時発火させ、周期送信は no data 条件で死活監視に使う。1 本のアラートで両方を兼ねると常時発火して破綻することを示している](/blogs/images/lte-m-liveness-two-layer-alerting.png)

分けて組むとこうなる。

- **Alert rule ①（異常検知）** — `di0` の変化を条件にする。接点変化は即時に飛ぶので、検知は送信間隔に依存しない。`for: 0` で 1 回で鳴らしてよい
- **Alert rule ②（死活監視）** — `Alert state if no data or all values are null` を Alerting にし、評価範囲を `now-(定期送信間隔 × 2〜3)` にする。ここは遅くて構わない

**1 本のアラートで両方を兼ねようとすると壊れる。** 「1 分データが来なければ鳴らす」を 1 本で組んだ場合、周期送信の間隔が 1 分を超えた時点で常時発火する。異常が起きていなくても鳴り続けるので、本当の異常が埋もれる。アラートは「異常が起きたら鳴る」ではなく「**異常が起きていないときは鳴らない**」を満たすように設計する。この観点は[SLI/SLO の設計](/blogs/posts/2026/05/sli-slo-sla-proposal/)と同じで、監視対象の正常な挙動を先に定義しないと閾値は決まらない。

## 死活監視の間隔を決める手順

一般化するとこうなる。LTE-M / LPWA の電池駆動機に限らず使える。

1. **デバイスの送信予算を確認する** — 電池なら総送信回数、給電なら制約なし。仕様が「回数」で書かれていたら、寿命は時間ではなく回数で決まっている
2. **必要な稼働期間から送信間隔を割り出す** — 200 回 ÷ 必要日数。ここで間隔が決まる。逆ではない
3. **その間隔を無通信タイマーと比べる** — 間隔のほうが長ければ、`Deleted` → `Created` が毎周期出るのが正常な状態になる。plan-D なら 1 時間が境界
4. **死活監視の閾値を間隔の 2〜3 倍に置く** — 1 回の取りこぼしで鳴らさない。検知遅延はここで確定する
5. **その遅延で困る要求を、別のアラートに分離する** — 即時性が要るものは、定期送信ではなくイベント駆動の送信（接点変化など）で拾う

順番が大事だ。「1 分ごとに知りたい」という要求から出発すると、デバイスが選べなくなる。**送信予算から出発すると、間隔が決まり、検知遅延が決まり、そこで足りない部分だけをイベント駆動で埋める**という設計になる。

## まとめ

- Lagoon 3 の `Alert state if no data or all values are null` で「データが来ない」はアラートにできる。`Evaluate every` は 10 秒の倍数
- ただし評価範囲をプランのデータ取得間隔（Free 60 秒 / Maker 30 秒 / Pro 5 秒）より `now` に近く取ると No data 判定になる
- SmartFitPRO Basic の電池予算は「単 4 形乾電池で 200 回程度 LTE 送信」。**1 分間隔なら 3.3 時間で終わる**
- 電池寿命は経過時間ではなく送信回数で決まる（待機 100μA、通信時 300mA）
- 電池で実用的な間隔は必ず plan-D の無通信タイマー 1 時間より長いので、`Deleted` → `Created` が毎周期出るのは正常
- 1 分粒度が要るなら Standard / Advance の給電モデル。待機電流が Basic の 350 倍あり、電池は選択肢に入らない代わりに間隔を詰められる
- 異常検知（接点変化送信・即時）と死活監視（周期送信・遅い）は別の Alert rule にする

「1 分間通信がなかったら」という要求は、たいていの場合「異常を 1 分以内に知りたい」の言い換えだ。そして接点変化送信を使えば、死活監視を 1 分にしなくてもそれは満たせる。要求を実装に翻訳する前に、要求そのものを分解したほうが早い。

## 参考リンク

- [Alert を管理する: Alert rule を設定する | SORACOM Lagoon 3](https://users.soracom.io/ja-jp/docs/lagoon-v3/setup-alert-rule/)
- [はじめに: SORACOM Lagoon 3 のプランとライセンスパック | SORACOM Lagoon 3](https://users.soracom.io/ja-jp/docs/lagoon-v3/plans/)
- [はじめに: サブスクリプションの種類 | SORACOM Air for セルラー](https://users.soracom.io/ja-jp/docs/air/subscriptions/)
- [リファレンス: ルールタイプ一覧 | イベントハンドラー](https://users.soracom.io/ja-jp/docs/event-handler/rules/)
- [SmartFitPRO 取扱説明書（旭光電機）](https://www.kyokko.co.jp/wp/wp-content/uploads/2022/09/SmartFitPRO_%E5%8F%96%E6%89%B1%E8%AA%AC%E6%98%8E%E6%9B%B8-1.pdf)
- [IoT ゲートウェイ SmartFitPRO Basic スターターキット | SORACOM IoT ストア](https://soracom.jp/store/23202/)
- [SmartFitPRO | 旭光電機株式会社](https://www.kyokko.co.jp/iot-solution/smartfit-pro)
