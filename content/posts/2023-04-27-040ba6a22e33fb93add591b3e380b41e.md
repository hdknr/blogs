---
title: "Django: Template: QuerySetの指定したindexのインスタンスの参照"
date: 2023-04-27
lastmod: 2023-04-27
draft: false
gist_id: "040ba6a22e33fb93add591b3e380b41e"
gist_url: "https://gist.github.com/hdknr/040ba6a22e33fb93add591b3e380b41e"
categories: ["Web開発"]
tags: ["python", "django"]
---

## Django: Template: QuerySet の指定した index のインスタンスの参照

- 配列ではないので、数値でインデクシングできない

```
{{ e.expenseitem_set.0 }} {# NG #}
```

### １件目, 最後

```
{{ e.expenseitem_set.first.account_name }}
{{ e.expenseitem_set.last.account_name }}
```

### テンプレートフィルター

```py
from django import template

register = template.Library()

@register.filter
def index(queryset, i):
    return queryset[i]
```

```html
{% load expenses %} {{ e.expenseitem_set|index:1 }}
```

### QuerySet のインクシング

- list のように `-1` などの Python のインデクシングはできない (するには list(queryset) で list に変換する)
